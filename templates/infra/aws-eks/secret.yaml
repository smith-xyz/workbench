# Do not commit real credentials; use sealed-secrets, External Secrets Operator, or inject at deploy time.
apiVersion: v1
kind: Secret
metadata:
  name: __SERVICE__-secret
type: Opaque
stringData:
  API_KEY: "REPLACE_ME"
  DATABASE_URL: "postgresql://user:password@host:5432/dbname"
