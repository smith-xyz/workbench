# Do not commit real credentials; use sealed-secrets, External Secrets Operator, or inject at deploy time.
apiVersion: v1
kind: Secret
metadata:
  name: __SERVICE__-secret
  labels:
    app.kubernetes.io/name: __SERVICE__
type: Opaque
stringData:
  # PLACEHOLDER — replace before applying to a real cluster; do not commit real values.
  API_KEY: "REPLACE_ME"
  DATABASE_URL: "REPLACE_ME"
